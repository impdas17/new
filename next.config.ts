import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* removed output: standalone to test if we can fix the bug in standard Next config first */
};

export default nextConfig;
